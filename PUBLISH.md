
Set correct `version` in `package.json` and update `CHANGELOG.md`. And then set correct npm-registry api in `.npmrc` file, (e.g. `//registry.npmjs.org/:_authToken=npm_VKT0kuGEvttKJD...mtrCQ70AuMJx`) and run:

```
$ npm run build

$ npm publish
```
